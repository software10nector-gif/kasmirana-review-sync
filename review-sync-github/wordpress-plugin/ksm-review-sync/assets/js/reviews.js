/* Reserved for a future swipeable slider on mobile — the CSS grid already
   reflows to 1 column on small screens without any JS needed. */
document.addEventListener('DOMContentLoaded', function () {
    var track = document.querySelector('.ksm-mp-reviews__track');
    if (!track) return;
});
